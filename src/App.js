/* ============================================================
   ENTRY POINT — Bootstrap the Valen app
   ============================================================ */

import { render, Component } from 'valen';
import Birthday from './components/Birthday.js';

render(Birthday);