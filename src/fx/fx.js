/* ============================================================
   FX — Shared confetti effects API
   Confetti.js mutates these functions at runtime.
   ============================================================ */

export const FX = {
  rain() {},
  burst() {}
};
